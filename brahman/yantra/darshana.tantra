tantra darshana

  -- node inspection: takes a name, returns formatted node info.
  -- render-node does the heavy lifting: lookup, edge rendering, domain info.

  inputs
    r-name  string

  let
    result = render-node r-name

  return
    result  string

done
