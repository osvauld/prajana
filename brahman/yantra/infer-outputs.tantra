tantra infer-outputs

  -- returns the phala (output) edges of a node, excluding domain-* targets.
  -- these are the conceptual outputs produced by the node.

  inputs
    node  string

  let
    phala   = walk node "phala"
    result  = filter phala (fn t -> not (starts-with t "domain-"))

  return
    result  list

done
