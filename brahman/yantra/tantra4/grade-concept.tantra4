-- extracts first user satya concept name from a grade.
(tantra grade-concept (grade)
  (find-map grade (fn [s, e, o] ->
    cond (and (eq e "satya") (not (is-system-concept s))) s
    otherwise _none)))
