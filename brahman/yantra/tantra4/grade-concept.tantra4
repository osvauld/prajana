-- extracts first user satya concept name from a grade.
(tantra grade-concept (grade)
  (find-map grade (fn t ->
    cond (and (eq (triple-edge t) "satya") (not (is-system-concept (triple-subj t))))
      (triple-subj t)
    otherwise _none)))
