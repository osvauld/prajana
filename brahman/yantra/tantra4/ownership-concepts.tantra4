-- collects concepts owned by entities via shashthi-vibhakti.
(tantra ownership-concepts (graph)
  (reduce graph [] (fn acc tri ->
    cond (and (eq (nth tri 1) "shashthi-vibhakti")
              (neq (nth tri 2) "shashthi-vibhakti"))
      (append acc [(str tri 0)])
    otherwise acc)))
