tantra lens-equation

  inputs
    object-distance  float  metre
    focal-length     float  metre

  let
    image-distance = div (mul object-distance focal-length) (sub object-distance focal-length)

  return
    image-distance  float  metre

done
