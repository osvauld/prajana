tantra projectile-range

  inputs
    velocity  float  metre-per-second  purva
    angle     float  radian

  let
    R = div (mul (power velocity 2.0) (sin (mul 2.0 angle))) gravity

  return
    R  float  metre

done
