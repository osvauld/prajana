tantra max-height

  inputs
    velocity  float  metre-per-second  purva
    angle     float  radian

  let
    h = div (mul (power velocity 2.0) (power (sin angle) 2.0)) (mul 2.0 gravity)

  return
    h  float  metre

done
