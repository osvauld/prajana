tantra test-find-context-active
  -- graph with satya triple → find-context returns that concept as active
  let
    g      = [["mass", "satya", "mass"]]
    ctx    = find-context g
    active = nth ctx 0
    result = eq active "mass"
  return
    result  bool
done
