tantra test-debug-node-slokas
  -- debug: what does node-slokas return for verb-has?
  let
    gnode  = lookup-word "has"
    slokas = node-slokas (to-string gnode)
    n      = length slokas
    ok     = gt n 0
  return
    ok  bool
done
