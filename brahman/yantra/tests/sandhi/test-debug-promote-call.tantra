tantra test-debug-promote-call
  -- debug: does calling sandhi-viveka with a single triple even work?
  -- use a satya triple which should pass through unchanged
  let
    g   = [["mass", "satya", "mass"]]
    r   = sandhi-viveka g
    ok  = eq (length r) 1
  return
    ok  bool
done
