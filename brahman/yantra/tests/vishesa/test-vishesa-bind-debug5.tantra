tantra test-vishesa-bind-debug5
  -- confirm: 3-element list works, 2-element does not
  let
    two   = ["a", "b"]
    three = ["a", "b", "c"]
    len2   = length two
    len3   = length three
  return
    len3  float
done
