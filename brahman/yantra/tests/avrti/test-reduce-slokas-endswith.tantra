tantra test-reduce-slokas-endswith
  let
    s = node-slokas "verb-has"
    r = reduce s "" (fn a x ->
      cond (ends-with x "-sthita") x
      otherwise a)
    ok = eq r "shashthi-vibhakti-sthita"
  return
    ok  bool
done
