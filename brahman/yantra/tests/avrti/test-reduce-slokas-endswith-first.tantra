tantra test-reduce-slokas-endswith-first
  -- does reduce+cond at least find the first -sthita match?
  let
    s = node-slokas "verb-has"
    r = reduce s "" (fn a x ->
      cond (ends-with x "-sthita") x
      otherwise a)
    ok = ends-with r "-sthita"
  return
    ok  bool
done
