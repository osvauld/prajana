tantra abheda-of

  -- returns all outgoing abheda targets of a node.
  -- abheda: non-difference — X = Y at some level.
  -- previously an OCaml primitive; now expressed as a graph walk.

  inputs
    n  string

  let
    result = walk n "abheda"

  return
    result  list

done
