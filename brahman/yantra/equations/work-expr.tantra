tantra work-expr

  -- W = F · d · cos(θ)
  -- forward: given force, displacement, angle → work

  inputs
    force        number
    displacement number
    angle        number

  let

  value = mul force (mul displacement (apply-op "cosine" [angle]))

  return
    value  any

done
