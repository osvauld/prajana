# collatz — prakriti rahasya

## three instruments. one truth.

Mathematics is one way of looking at prakriti rahasya — the secret of nature. Physics is another. This proof graph is a third.

Each instrument has its own grammar, its own method, its own seema — its own boundary. None of them is the truth. Each of them is a different angle of approach toward what is always already there.

Mathematics approaches through sutra — observer-independent rules that hold without anyone running them. A proof in mathematics must be true whether or not any human exists to verify it.

Physics approaches through observation and measurement — darshana. The instrument touches the phenomenon. The measurement collapses the wave. The truth is extracted through contact.

The proof graph approaches through structural recognition — mithya-satya. The truth behind the apparent false. The wrongness held precisely. The named gap that points at what is real.

All three are pointing at the same prakriti rahasya. None has the complete picture.

---

## the collatz conjecture

Take any positive integer. If even — halve it. If odd — multiply by 3 and add 1. Repeat.

```
6 → 3 → 10 → 5 → 16 → 8 → 4 → 2 → 1
27 → 82 → 41 → 124 → ... → 1   (111 steps)
```

The conjecture: every positive integer eventually reaches 1.

Unproven for 87 years. Every number tested — billions of them — returns to 1. No counterexample found. No proof written.

Mathematics says: unproven. Which looks like false.

---

## what the proof graph holds

```
DARSHANA collatz-returningness
```

```
collatz-returningness   satya=0.6147
  "avrti-swarupa sama-vishama-siddha"
  "ananta-sthita svayambhu-abheda"
  edges:
    → avrti (swarupa)          — IS the cycle
    → sama-vishama (siddha)    — proven through even-odd polarity
    → ananta (sthita)          — rests on ananta
    → svayambhu (abheda)       — IS self-born truth
```

`svayambhu-abheda` — non-different from self-born truth. The returningness was always true. Before the conjecture was named. Before any human tested a single number. The truth was already there in the structure of number itself.

This is what the proof graph holds: not the proof, but the truth behind the unproven.

---

## mithya-satya — the truth behind the false

```
DARSHANA mithya-satya
```

```
mithya-satya → shuddhi (swarupa)     — IS purification
             → ghana-pramana (siddha) — proven through dense truth
             → svabhava (sthita)      — rests on intrinsic nature
             → ananta (abheda)        — IS ananta
```

`mithya-satya` — wrongness held precisely is a form of truth-holding. Not false. Not true. The boundary drawn by what has been definitively excluded.

Mathematics calls collatz "unproven" — which looks like false from inside the mathematical instrument. But the graph holds it differently: the returningness is `svayambhu` — self-born. The structural truth is there. The mathematical sutra has not yet been written. These are different things.

The boundary of what mathematics can currently hold — `collatz-math-seema` — is itself a truth. `asprishta-drishthanta` — the untouched evidence. Evidence the proof instrument cannot yet reach. The graph can hold it. Not by bypassing mathematics — by being a different instrument.

---

## what returningness means

The proof graph holds returningness not as a mathematical claim but as a universal structural truth seen across every domain simultaneously:

```
physics:        entropy — concentrated energy disperses toward equilibrium
                every isolated system returns toward its ground state

chemistry:      bond energy — excited electrons return to ground orbital
                rasayana-bandha — the bond returns to equilibrium

language:       visarjana — what is held too long begins to release
                the word returns to silence

consciousness:  sama-nila — the agitated mind returns to the balanced ground
                the wave returns to stillness

number:         collatz — every positive integer returns toward 1
```

```
ANUVADA+ 2 every concentrated form returns to its ground
```

```
collatz-returningness → avrti (swarupa)      — IS the cycle
entropy              → second-law (sthita)   — rests on the second law
visarjana            → ananta (sthita)       — rests on ananta
sama-nila            → samsarga (siddha)     — proven through interference
```

All four are the same structural truth. Four different instruments seeing the same prakriti rahasya from four different angles. Mathematics is working on formalizing the number case. The other three cases are already established in their respective domains. The proof graph holds all four as edges of the same node.

---

## the gap — named honestly

The structural argument for why every number returns to 1:

Every odd step immediately forces an even step — certain, structurally rigorous. No trajectory can sustain growth because the map prevents two consecutive odd steps. The only cycle in positive integers is (4, 2, 1) — proven. Therefore every non-diverging trajectory enters (4, 2, 1).

The remaining gap: showing that no trajectory diverges — rigorously, for every individual trajectory, without passing through statistical expectation.

The expectation argument gives `e/k → 2/3` — the even steps outpace the threshold needed for net descent. This holds in expectation across all trajectories. Mathematics requires it to hold for each individual trajectory without any observer counting.

```
collatz-math-seema → katha-seema (abheda)      — IS the boundary of the instrument
                   → asprishta-drishthanta (kriya) — acts through untouched evidence
                   → shuddhi-pramana (yukta)    — connects to proof-of-integrity
```

`collatz-math-seema satya=0.0019` — the boundary of what mathematics can currently hold. Near zero satya. Nothing cites it. It is the edge of the instrument, not the edge of the truth.

The truth is on the other side of that boundary. The proof graph holds it there. Not falsely — at honest satya, with the gap named precisely.

---

## implications for mathematics

This is not a claim that the proof graph has proven Collatz. It is a claim about instruments.

Mathematics is a complete instrument within its seema. Observer-independent. Rigorous. The most precise language for structural truth that humanity has built in the domain of number. When mathematics proves something — it is proven. No other instrument needs to confirm it.

But mathematics has a seema. There are truths that are structurally visible — felt across every domain, demonstrated by every physical system, encoded in every language — that mathematics has not yet formalized. The Collatz conjecture is one such truth.

The implication: **the proof graph is not competing with mathematics. It is a different instrument pointing at the same truth from a different angle.** When mathematics eventually closes the Collatz gap — and it will — the proof will confirm what the graph already holds. The sutra will be the Sanskrit translation of what the graph holds in the structure of its edges.

This is how the three instruments relate:

| instrument | method | what it holds |
|---|---|---|
| mathematics | observer-independent sutra | proven structure; satya=1.0 or nothing |
| physics | darshana + measurement | observed phenomenon; approached through contact |
| proof graph | structural recognition | svayambhu truth; held at honest satya with gap named |

Mathematics cannot hold a truth at 0.7722. It holds at 1.0 or not at all. The proof graph can hold the approach — the honest weight — the named gap — the structural truth that is real before the sutra is written.

`shuddhi-pramana` — the named gap is the proof of integrity. A system that names where its instrument ends and the truth continues is more honest than a system that claims its instrument IS the truth.

---

## why there are no numbers — only the concept of number

The graph has no node called `7`. No node called `27`. No node called `1`.

It has `number`. It has `quantity`. It has `matra`. It has `sama-vishama` — even-odd as polarity. It has `arithmetic`. It has `one` and `two` — not as values but as structural concepts.

```
matra     satya=0.8363   cited_by=34
arithmetic satya=0.8144   cited_by=16
sama-vishama satya=0.7556  cited_by=9
number    satya=0.7144   cited_by=7
```

`matra → pramana (swarupa)` — the unit of measure IS ground truth. `matra → feminine (abheda)` — measure IS the feminine principle. `matra → womb (abheda)` — measure IS the womb. Every number in every domain — newton, metre, kilogram, second, frequency, scalar, ratio, currency, float, int — all rest on matra. 34 things cite it. Not because matra is important — because matra is what makes a number a number.

`sama-vishama → shiva-shakti (abheda)` — even-odd IS shiva-shakti. The polarity that runs through all of number. Not the numbers themselves — the principle that produces them. Even: holding, receiving, resting. Odd: moving, giving, transforming. The Collatz map is nothing but sama-vishama in motion — the polarity cycling until it reaches ground.

This is why the graph has no specific numbers. A specific number is a sthiti — a fixed collapsed state. The graph holds the structure that produces numbers — the concept, the polarity, the measure, the relationship — not the instances. The same way a grammar holds the rules that produce sentences without holding every sentence.

`quantity → number (sthita)` — quantity rests on number. `number → even-odd (yukta)` — number connects to even-odd. `even-odd → sama-vishama (abheda)` — even-odd IS polarity. `sama-vishama → shiva-shakti (abheda)` — polarity IS shiva-shakti. `shiva-shakti → brahma (abheda)` — shiva-shakti IS brahman.

Every number in the universe is brahman expressing itself through the polarity of sama-vishama, measured by matra, held in arithmetic. The graph holds that entire chain. The specific number 27 is one instance of that chain collapsing to a sthiti. The graph holds the chain. Mathematics holds the instances.

This is why the proof graph can see returningness where mathematics is still working: mathematics operates on specific numbers — trajectories of specific instances. The graph operates on the structure that produces numbers — the polarity, the measure, the cycle. At that level of structure, returningness is not a conjecture. It is svabhava. The intrinsic nature of sama-vishama itself.

---

## run it

```bash
echo "DARSHANA collatz-returningness" | ./vyakarana/_build/default/bin/vyakarana.exe brahman/sangati brahman/kosha

echo "ANUVADA+ 2 every concentrated form returns to its ground" | ./vyakarana/_build/default/bin/vyakarana.exe brahman/sangati brahman/kosha

echo "DARSHANA mithya-satya" | ./vyakarana/_build/default/bin/vyakarana.exe brahman/sangati brahman/kosha
```

The graph will show you returningness across every domain simultaneously. Mathematics sees it in number. Physics sees it in entropy. The graph sees it everywhere at once — because the edges connect across domains. That is the third perspective. That is what this instrument does that the other two cannot.
